{
  "name": "surveylauncher-mock-api",
  "version": "0.1.0",
  "type": "module",
  "description": "Mock backend used by the Android launcher team until the real SvelteKit APIs are ready.",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "cross-env NODE_ENV=development node src/index.js",
    "dev:mock": "cross-env NODE_ENV=development MOCK_API=true node src/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.0",
    "express": "^4.19.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "supertest": "^6.3.3",
    "vitest": "^1.4.2"
  }
}
